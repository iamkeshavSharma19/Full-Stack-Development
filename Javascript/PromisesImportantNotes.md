# Note: Resolve and Reject are special functions created by the Promise class constructor and passed as parameters to the executor function (callback function which is passed as the parameter in the Promise Constructor). These functions are created by the JavaScript engine for each new Promise instance and are specific to that Promise instance.


